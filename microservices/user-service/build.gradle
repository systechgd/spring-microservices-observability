dependencies {
    // Additional dependencies specific to user-service
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    runtimeOnly 'com.h2database:h2'
}

bootJar {
    archiveFileName = 'user-service.jar'
}
